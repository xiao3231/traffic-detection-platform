# 毕业实习日记填写稿（2026.3.9—3.27）

> 说明：根据《实习项目.docx》中「广告数据分析平台」「bidder_ssp 协助开发」及前后端学习笔记整理。请按模板每格粘贴对应日期内容；封面实习单位、姓名、学号等请自行填写。  
> 建议实习单位写：**青岛理工大学 / 校内实习（或实际单位全称）**；岗位：**软件开发（网络空间安全方向）**；场所：**校内机房或远程办公**。

---

## 2026年3月9日（周一）

**实习时间：** 9:00—17:30  
**实习场所：** 校内实习工位（PyCharm / VS Code）  
**实习岗位：** 软件开发助理  
**实习方式：** 在指导教师布置下熟悉实习任务书，搭建 Python、Node.js 环境  

**实习内容：** 阅读网络空间安全专业实习要求，明确需完成日记与报告。领取并梳理自主实习项目「广告收入数据分析平台」，了解前后端分离结构：前端 Vite + Vue，后端 Python + Tornado，接口统一前缀 `/api`。本地安装 Python 3、Node.js，克隆项目后执行 `npm install` 与 `pip install -r requirements.txt`，成功启动前端 dev 服务。  

**问题与解决：** 首次启动前端报依赖缺失。按报错执行 `npm install` 补全 `node_modules`；查阅 Vite 文档确认入口为 `index.html` 加载 `main.js`，理清与 Vue 的分工。

---

## 2026年3月10日（周二）

**实习时间：** 9:00—17:30  
**实习场所：** 校内实习工位  
**实习岗位：** 前端开发学习  
**实习方式：** 跟读项目代码 + 笔记整理  

**实习内容：** 学习 Vue 3 应用启动流程：`createApp(App)` → `use(router)` / `use(pinia)` / `use(ElementPlus)` → `mount('#app')`。理解 `router-view` 根据 URL 渲染不同页面。阅读 `frontend/src/api/index.js` 中 axios 实例配置：`baseURL: '/api'`、`timeout: 10000` 及响应拦截器自动返回 `response.data`。在浏览器访问页面，观察 Network 面板中 `/api` 请求。  

**问题与解决：** 不清楚 npm 与 Node 关系。总结：npm 管理 `package.json` 依赖，包装安装在 `node_modules`；Node 提供运行环境，类比 Python 与 pip。

---

## 2026年3月11日（周三）

**实习时间：** 9:00—17:30  
**实习场所：** 校内实习工位  
**实习岗位：** 后端开发学习  
**实习方式：** 跟踪一次完整 API 调用链  

**实习内容：** 从 `getRevenueStats` 入手，梳理前端 `data.js` → axios GET `/api/revenue-stats` → Tornado 路由表 `make_app()` → `RevenueStatsHandler.get()` → `data_service.get_revenue_stats()`。理解 Handler 中参数校验、`json_response` 统一返回格式。后端按日期遍历 CSV，统计 Revenue 有效行数并组装列表返回。  

**问题与解决：** 修改后端后页面无变化。发现 Tornado 开启 `autoreload` 需保存文件触发；部分情况需手动重启进程，建立「改代码—看终端—刷新页面」习惯。

---

## 2026年3月12日（周四）

**实习时间：** 9:00—17:30  
**实习场所：** 校内实习工位  
**实习岗位：** 数据分析功能协助  
**实习方式：** 在指导下小改 service 层逻辑  

**实习内容：** 继续完善收入统计模块，理解 `_find_csv_for_date` 按日期匹配 CSV 文件、`_count_revenue_rows` 统计行数。协助调整日期范围内「无文件日期返回 null」的展示，使前端图表能区分缺失数据与零值。练习使用 `Promise.all` 并行请求 `getRevenueStats` 与 `getRevenueTable`，缩短页面加载时间。  

**问题与解决：** 并行请求其一失败导致整页报错。为单个请求增加 try/catch，失败时仅提示对应模块，避免整页白屏。

---

## 2026年3月13日（周五）

**实习时间：** 9:00—17:30  
**实习场所：** 校内实习工位  
**实习岗位：** 全栈联调  
**实习方式：** 前后端联调 + 记录接口文档  

**实习内容：** 配置 Vite 代理，将 `/api` 转发至 Tornado 监听端口，解决开发环境跨域。梳理已存在接口：hello、revenue-stats、revenue-table、category-analysis。整理「路径—方法—参数—返回」简易表，作为组内交接说明。了解 RESTful 风格：资源用名词、查询用 GET、错误信息放在 JSON 的 `message` 字段。  

**问题与解决：** 浏览器直接访问后端端口与走代理结果不一致。统一前端只访问相对路径 `/api`，避免写死 `localhost:8888`，减少环境差异。

---

## 2026年3月14日（周六）

**实习时间：** 9:30—16:00  
**实习场所：** 校内实习工位  
**实习岗位：** 自主学习  
**实习方式：** 专题笔记（HTTP / 鉴权基础）  

**实习内容：** 结合实习任务书中「安全监测、日志分析」要求，自学 HTTP 状态码、Cookie 与 Session 区别。对照广告平台与后续协助项目，理解 JWT 无状态鉴权思路。阅读 MDN 与实验文档，记录 HTTPS、同源策略与 Vite proxy 的关系，为下周参与 bidder 项目做准备。  

**问题与解决：** 概念易混。用表格归纳：Session 存服务端、Cookie 带 id；JWT 自包含签名、适合前后端分离 API。

---

## 2026年3月15日（周日）

**实习时间：** 休息（可选补记：14:00—17:00 环境整理）  
**实习场所：** 宿舍 / 机房  
**实习岗位：** —  
**实习方式：** 整理一周笔记  

**实习内容：** 整理本周前后端分离学习笔记：Vite 负责构建与热更新，Vue 负责组件与路由，Tornado 负责路由与 Handler，service 层负责业务与读 CSV。列出尚未掌握项：装饰器、async/await、MongoDB 查询。预习 Element Plus 表格与表单组件文档。  

**问题与解决：** 无。

---

## 2026年3月16日（周一）

**实习时间：** 9:00—17:30  
**实习场所：** 实习单位远程协作（或校内）  
**实习岗位：** 后端开发协助（bidder_ssp_api）  
**实习方式：** 阅读现有工程 + 本地跑通  

**实习内容：** 开始协助 bidder_ssp_api 项目。了解技术栈：Python 3.13、Tornado、Seal 配置框架、Motor 异步访问 MongoDB、PyJWT 与 bcrypt。阅读 `conf.yaml` 与 seal.conf，理解 7 个 collection 的业务含义。在 Docker 或本地启动 MongoDB，尝试启动服务并访问健康检查/登录接口。  

**问题与解决：** MongoDB 连接失败。检查 URI、端口 27017 是否启动、防火墙；对照配置示例修正数据库名与账号。

---

## 2026年3月17日（周二）

**实习时间：** 9:00—17:30  
**实习场所：** 实习工位  
**实习岗位：** 后端开发协助  
**实习方式：** 跟读鉴权与 Handler 代码  

**实习内容：** 学习用户登录流程：密码 bcrypt 校验、通过后签发 JWT。理解 BaseHandler 中统一鉴权装饰或前置校验，未带 token 请求返回 401。对照网络空间安全要求，记录「密码不明文存储」「token 过期时间」等安全点。协助排查一条列表接口分页参数错误导致的 400。  

**问题与解决：** token 过期后前端仍带旧 token。建议前端在 401 时跳转登录页并清除 localStorage，与负责人确认后记录为待办。

---

## 2026年3月18日（周三）

**实习时间：** 9:00—17:30  
**实习场所：** 实习工位  
**实习岗位：** 前端开发协助（bidder_ssp_ui）  
**实习方式：** 阅读 Vue 3 + Element Plus 工程  

**实习内容：** 转入 bidder_ssp_ui：Vue 3.5、Vite 6、Element Plus、Pinia、Vue Router。阅读 `main.js` 全局注册图标与插件；分析路由表 `/login` 与主布局 `MainLayout` 下子路由（报表、Ad Unit、Applications、Account 等）。理解 `meta.requiresAuth` 与路由守卫控制未登录跳转。  

**问题与解决：** 路径别名 `@` 解析失败。检查 `vite.config.js` 中 `resolve.alias` 是否配置 `@` → `src`。

---

## 2026年3月19日（周四）

**实习时间：** 9:00—17:30  
**实习场所：** 实习工位  
**实习岗位：** 前端开发协助  
**实习方式：** 小功能修改 + 联调  

**实习内容：** 在 Account 管理页熟悉表格、弹窗、表单校验写法。练习 `router.push` 编程式导航与 `await api.xxx()` 调用后端。协助修改某列表页查询按钮：将日期参数通过 query 传给后端，展示 loading 状态。加深 axios 与 fetch 区别理解（拦截器、baseURL、超时）。  

**问题与解决：** 表格数据更新后仍显示旧数据。在请求成功后重新调用 `loadList()`，并注意 Vue 响应式需替换数组引用而非仅改下标。

---

## 2026年3月20日（周五）

**实习时间：** 9:00—17:30  
**实习场所：** 实习工位  
**实习岗位：** 全栈协助  
**实习方式：** 联调 + 日志排查  

**实习内容：** 前后端联调 bidder 报表接口：核对请求头 Authorization、CORS 与代理配置。学习在后端打印结构化日志便于排错。了解项目 Docker 基于 `seal:latest` 部署流程，阅读 Dockerfile 初稿。对比广告分析平台与 bidder 项目架构异同（均前后端分离，后者重 MongoDB 与 JWT）。  

**问题与解决：** 跨域预检 OPTIONS 失败。后端补充允许方法与 Header，开发阶段继续使用 Vite proxy 转发 `/api`。

---

## 2026年3月21日（周六）

**实习时间：** 9:30—15:30  
**实习场所：** 校内  
**实习岗位：** 自主学习  
**实习方式：** 复习 + 实习任务书对照  

**实习内容：** 对照实习任务书第 2、3 条，总结本周工作：参与日常开发协助、接口联调、接触 MongoDB 与日志排查，属网络安全运维中的「监测与策略」延伸（业务系统安全）。整理 Python 装饰器、生成器简要笔记。预习 PostgreSQL/MySQL 与 REST 分页规范，列入后续学习计划。  

**问题与解决：** 无。

---

## 2026年3月22日（周日）

**实习时间：** 休息  
**实习场所：** —  
**实习内容：** 撰写本周小结（约 300 字），为实习报告「实习过程」积累素材：完成两个 Web 项目的结构理解与环境搭建，具备基本前后端联调能力。  

**问题与解决：** 无。

---

## 2026年3月23日（周一）

**实习时间：** 9:00—17:30  
**实习场所：** 实习工位  
**实习岗位：** 后端开发协助  
**实习方式：** 协助改 service + 单元自测  

**实习内容：** 协助完善 bidder 某查询接口：增加过滤条件校验、统一 `code/message/data` 响应。练习 Motor 异步 `find` 与 `to_list` 用法。回顾 Tornado 与 Flask 差异（异步 IO、路由注册方式）。思考高并发场景下缓存（Redis）可减轻 MongoDB 压力，记录为报告「建议」素材。  

**问题与解决：** 异步 Handler 中误用同步 pymongo 导致阻塞。改为 Motor 客户端，并在文档中标注「异步链路勿混同步库」。

---

## 2026年3月24日（周二）

**实习时间：** 9:00—17:30  
**实习场所：** 实习工位  
**实习岗位：** 前端开发协助  
**实习方式：** UI 调整 + 自测  

**实习内容：** 优化登录页与表格页样式，统一 Element Plus 主题色与间距。为表单增加必填与长度校验，减少无效请求。学习 Pinia 存储用户信息与 token，退出登录时清空 store。编写简单自测清单：登录、列表、分页、退出。  

**问题与解决：** 刷新页面后登录态丢失。将 token 持久化至 sessionStorage，并在路由守卫中恢复，与导师确认安全策略（关闭浏览器即失效）。

---

## 2026年3月25日（周三）

**实习时间：** 9:00—17:30  
**实习场所：** 实习工位  
**实习岗位：** 综合实践  
**实习方式：** 文档整理 + 小 bug 修复  

**实习内容：** 整理两个项目的目录说明与启动命令（前端 `npm run dev`、后端启动脚本、MongoDB 依赖）。协助修复广告平台某日期边界「结束日期早于开始日期」未校验问题。阅读 OpenAPI/Swagger 概念，了解接口文档对团队协作的重要性。  

**问题与解决：** CSV 日期格式不统一导致统计异常。在 service 层统一 `strptime` 格式，非法文件跳过并写日志。

---

## 2026年3月26日（周四）

**实习时间：** 9:00—17:30  
**实习场所：** 实习工位  
**实习岗位：** 实习总结准备  
**实习方式：** 回归测试 + 报告提纲  

**实习内容：** 对本周修改功能做回归：登录、报表查询、用户列表。开始撰写实习报告提纲：前言（实习环境与目的）、实习内容（广告平台 + bidder 协助）、收获（前后端分离、JWT、MongoDB、工程规范）、建议（加强自动化测试与 CI）。向指导教师汇报进度，确认 3.27 前完成日记收尾。  

**问题与解决：** 某接口偶发 500。查后端日志为 ObjectId 格式错误，前端传参增加校验，后端 `try/except InvalidId` 返回 400。

---

## 2026年3月27日（周五）

**实习时间：** 9:00—17:00  
**实习场所：** 校内实习工位  
**实习岗位：** 实习收尾  
**实习方式：** 总结汇报  

**实习内容：** 实习最后一天：完善实习日记与报告初稿，核对封面实习时间 2026.3.9—3.27。总结十九天收获：①掌握 Vite+Vue3 前端工程化基本流程；②理解 Tornado 路由与 Handler 分层；③熟悉 axios 封装、JWT 鉴权与 MongoDB 文档库使用；④具备基本联调与日志排错能力。听取指导教师意见，后续将结合毕设「基于 Scapy 与机器学习的流量分析平台」继续深化网络安全实践（与本次 Web 开发能力互补）。  

**问题与解决：** 报告字数不足。按模板补充「实习环境」单位简介、各模块截图说明及致谢，计划扩写至 5000 字以上。

---

## 模板剩余空栏（若还有 5 格）

可留空，或抄写 **3月15日、3月22日** 休息/整理笔记内容作为补页。

---

## 封面建议填写

| 项目 | 建议 |
|------|------|
| 实习单位 | 青岛理工大学（或实际实习企业全称） |
| 实习性质 | 毕业实习（分散/集中按学校要求） |
| 实习时间 | 2026.3.9 至 2026.3.27 |
| 指导教师 | 李道全 |
